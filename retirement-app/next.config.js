/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Retirement',
  assetPrefix: '/Retirement/',
  images: { unoptimized: true },
}

module.exports = nextConfig
