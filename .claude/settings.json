{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command'); if echo \"$cmd\" | grep -q 'git push'; then cd /home/user/program_plan && (npm --prefix server test 2>/dev/null || true) && (npm --prefix client test 2>/dev/null || true); fi",
            "timeout": 120,
            "statusMessage": "Running tests before push..."
          }
        ]
      }
    ]
  }
}
